{% extends 'backoffice/larp/base.html.twig' %}

{% import 'macros/ui_components.html.twig' as ui %}

{% block larp_title %}{{ (recruitment is defined and recruitment.id ? 'larp.recruitment.modify' : 'larp.recruitment.create')|trans }} - {{ larp.title }}{% endblock %}

{% block larp_content %}
<div class="tab-pane border border-top-0 p-3">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ ui.form_actions(
        recruitment is defined and recruitment.id ? 'save' : 'create',
        'bi-check-circle',
        path('backoffice_larp_story_marketplace_list', { larp: larp.id })
    ) }}
    {{ form_end(form) }}
</div>
{% endblock %}
