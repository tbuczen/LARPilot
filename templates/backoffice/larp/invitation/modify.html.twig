{% extends 'backoffice/larp/base.html.twig' %}

{% block larp_content %}
    {{ component('InvitationForm', {
        formData: form.vars.value,
        form: form,
    }) }}
{% endblock %}

{% block larp_title %}{{ (invitation is defined and invitation.id ? 'larp.invitation.modify' : 'larp.invitation.create')|trans }} - {{ larp.title }}{% endblock %}

