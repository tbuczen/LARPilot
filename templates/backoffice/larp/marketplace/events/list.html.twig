{% extends 'backoffice/larp/marketplace/_base.html.twig' %}

{% set active_tab = 'events' %}

{% block marketplace_content %}
    {% include 'includes/filter_form.html.twig' with { form: filterForm } %}

    {% if events is not empty %}
        <div class="table-responsive">
            <table class="table table-striped table-hover mb-0">
                <thead class="table-light">
                    <tr>
                        <th>{{ 'common.title'|trans }}</th>
                        <th>{{ 'common.description'|trans }}</th>
                        <th>{{ 'common.tags'|trans }}</th>
                        <th>{{ 'backoffice.marketplace.involved'|trans }}</th>
                        <th>{{ 'common.actions'|trans }}</th>
                    </tr>
                </thead>
                <tbody>
                    {% for event in events %}
                        <tr>
                            <td>
                                <a href="{{ path('backoffice_larp_story_event_modify', {
                                    larp: larp.id, event: event.id
                                }) }}">
                                    {{ event.title }}
                                </a>
                            </td>
                            <td>{{ event.description|sanitize_html|striptags|u.truncate(100, '...') }}</td>
                            <td>
                                {% for tag in event.tags %}
                                    <span class="badge bg-info">{{ tag.title }}</span>
                                {% endfor %}
                            </td>
                            <td>
                                {% if event.involvedCharacters|length > 0 %}
                                    {{ event.involvedCharacters|length }} {{ 'common.characters'|trans }}
                                {% else %}
                                    <span class="text-muted">{{ 'backoffice.marketplace.no_characters'|trans }}</span>
                                {% endif %}
                            </td>
                            <td>
                                <a href="{{ path('backoffice_larp_story_event_modify', {
                                    larp: larp.id, event: event.id
                                }) }}" class="btn btn-sm btn-primary">
                                    {{ 'common.edit'|trans }}
                                </a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>

        {% include 'includes/pagination.html.twig' with { pagination: events } %}
    {% else %}
        <p class="text-muted p-3">{{ 'common.empty_list'|trans }}</p>
    {% endif %}
{% endblock %}
