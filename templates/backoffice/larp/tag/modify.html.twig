{% extends 'backoffice/larp/base.html.twig' %}

{% block larp_title %}{{ (tag is defined and tag.id ? 'larp.tag.modify' : 'larp.tag.create')|trans }} - {{ larp.title }}{% endblock %}

{% block larp_content %}
    <div>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
